using UnityEngine;


public class GlowLimiter : MonoBehaviour
{
	/*
	public GlowEffect glow;

	private void Start()
	{
		glow = (base.gameObject.GetComponent("GlowEffect") as GlowEffect);
	}

	private void Update()
	{
		if (GameData.mGameSettings.mGraphicsLevel == DynamicOptions.Settings.GraphicsLevel.Low)
		{
			glow.enabled = false;
		}
		else
		{
			glow.enabled = true;
		}
	}
	*/
}
